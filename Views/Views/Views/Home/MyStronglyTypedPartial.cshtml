@model IEnumerable<string>

<div class="bg-info">
    To jest komunikat z widoku częsciowego
    <ul>
        @foreach(string str in Model)
        {
            <li>@str</li>
        }
    </ul>
</div>